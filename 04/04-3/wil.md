범위 자료형 : 어느 정도의 범위의 숫자를 자료로 생성
  range(A) : 0부터 A-1까지
  range(A,B) : A부터 B-1까지
  range(A, B, C) : A부터 B-1까지, 커지는 단위는 C

while : 특정 횟수만큼 반복
  for 처럼 사용 가능
  상태를 기반으로 반복
  시간을 기반으로 반복

break : 반복문 벗어남
continue : 다음 반복