재귀 함수 : 자기 자신을 호출하는 것
다만 기하급수적으로 같은 것을 많이 반복하는 경우 존재

파이썬은 함수 내부에서 함수 외부에 있는 변수 참조 못함
global 키워드 : 변수에 접근할 수 있도록 도와줌
메모화 : 이미 한 번 계산한 값을 저장

조기 리턴 : 흐름 중간에 return 사용

## 코드에 주석을 붙여주면 설명도 쉬움
## 또한 같은 값을 사용하면, 같은 변수로 묶어주면 유지보수도 쉬움